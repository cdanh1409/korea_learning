class Vocabulary {
  constructor(id, word, meaning, level, topicId) {
    this.id = id;
    this.word = word;
    this.meaning = meaning;
    this.level = level;
    this.topicId = topicId;
  }
}

module.exports = Vocabulary;
